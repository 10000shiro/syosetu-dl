from distutils.core import setup
setup(
  name = 'syosetu-dl',
  packages = ['syosetu-dl'], # this must be the same as the name above
  version = '1.0',
  description = 'A downloader for the japanese web novel website syosetu.com',
  author = 'manshiro',
  author_email = 'mail@manshiro.com',
  url = 'https://github.com/10000shiro/syosetu-dl', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['web novel', 'downloader'], # arbitrary keywords
  classifiers = [],
)